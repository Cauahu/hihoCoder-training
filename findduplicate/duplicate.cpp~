#include <iostream>
#include <string.h>

using namespace std;

int main(){
int duparray[length];
        memset(duparray,0,length);
        bool have_duplication = false;
        for(int i=0;i<length;++i){
            int cur = numbers[i];
            ++duparray[cur];
            if(duparray[cur] == 2){
                *duplication = cur;
                have_duplication = true;
                break;
            }
        }
	return 0;
}
